---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: runs.kato.zufardhiyaulhaq.com
spec:
  group: kato.zufardhiyaulhaq.com
  names:
    kind: Run
    listKind: RunList
    plural: runs
    singular: run
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.useCase
      name: UseCase
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              inputs:
                additionalProperties:
                  type: string
                type: object
              useCase:
                type: string
            required:
            - useCase
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              modelConfig:
                description: ModelConfig records which ModelConfig produced the summary.
                type: string
              phase:
                enum:
                - Succeeded
                - PartiallySucceeded
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              steps:
                items:
                  properties:
                    error:
                      type: string
                    name:
                      type: string
                    outcome:
                      enum:
                      - completed
                      - skipped
                      - failed
                      type: string
                    outputs:
                      description: Outputs holds all declared outputs of the step
                        (unfiltered).
                      x-kubernetes-preserve-unknown-fields: true
                    reason:
                      description: Reason explains skipped/failed outcomes.
                      type: string
                  required:
                  - name
                  - outcome
                  type: object
                type: array
              summary:
                type: string
              warning:
                description: |-
                  Warning is set when the summary could not be produced (LLM down, no
                  default ModelConfig, ...) while step outputs are still valid.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
