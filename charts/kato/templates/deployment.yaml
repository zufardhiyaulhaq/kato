apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kato.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kato.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: kato
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- range $k, $v := . }}
        {{ $k }}: {{ $v | quote }}
        {{- end }}
      {{- end }}
      labels:
        {{- include "kato.labels" . | nindent 8 }}
        {{- range $k, $v := .Values.podLabels }}
        {{ $k }}: {{ $v | quote }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "kato.name" . }}
      containers:
        - name: kato
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: KATO_NAMESPACE
              value: {{ .Release.Namespace }}
            - name: KATO_LISTEN_ADDR
              value: ":{{ .Values.service.port }}"
            - name: KATO_STEP_TIMEOUT
              value: {{ .Values.config.stepTimeout | quote }}
            - name: KATO_RUN_TTL
              value: {{ .Values.config.runTTL | quote }}
            - name: KATO_MAX_CONCURRENT
              value: {{ .Values.config.maxConcurrent | quote }}
            - name: KATO_METHOD_MAX_CONCURRENT
              value: {{ .Values.config.methodMaxConcurrent | quote }}
            - name: KATO_GC_INTERVAL
              value: {{ .Values.config.gcInterval | quote }}
            - name: KATO_RUN_RECONCILE_CONCURRENCY
              value: {{ .Values.config.runReconcileConcurrency | quote }}
            - name: KATO_RUN_MAX_DURATION
              value: {{ .Values.config.runMaxDuration | quote }}
          readinessProbe:
            httpGet: { path: /readyz, port: {{ .Values.service.port }} }
          livenessProbe:
            httpGet: { path: /healthz, port: {{ .Values.service.port }} }
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
