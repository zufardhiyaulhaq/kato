---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: usecases.kato.zufardhiyaulhaq.com
spec:
  group: kato.zufardhiyaulhaq.com
  names:
    kind: UseCase
    listKind: UseCaseList
    plural: usecases
    singular: usecase
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                type: string
              inputs:
                items:
                  description: InputDecl declares a UseCase input. v1 supports type
                    "string" only.
                  properties:
                    default:
                      description: Default is used when the caller omits this input.
                        Empty means no default.
                      type: string
                    name:
                      type: string
                    required:
                      type: boolean
                    type:
                      default: string
                      enum:
                      - string
                      type: string
                  required:
                  - name
                  type: object
                type: array
              steps:
                items:
                  description: Step is one method invocation in the flow.
                  properties:
                    forEach:
                      description: |-
                        ForEach references a list output of an earlier step, e.g.
                        "$(steps.crashing.pods)". When set, Method runs once per item and
                        $(item.<field>) is available in With (not in When).
                      type: string
                    maxItems:
                      description: MaxItems caps forEach iterations. Default 5; hard
                        ceiling 20.
                      type: integer
                    method:
                      type: string
                    name:
                      type: string
                    summaryFilter:
                      description: |-
                        SummaryFilter lists which outputs the LLM sees.
                        nil (omitted) = all outputs; empty list = none (audit-only step).
                      items:
                        type: string
                      type: array
                    when:
                      description: When is a CEL expression over $() references; absent
                        means run always.
                      type: string
                    with:
                      additionalProperties:
                        type: string
                      type: object
                  required:
                  - method
                  - name
                  type: object
                minItems: 1
                type: array
              summary:
                properties:
                  modelConfigRef:
                    description: ModelConfigRef names a ModelConfig; empty means the
                      default ModelConfig.
                    type: string
                  prompt:
                    type: string
                required:
                - prompt
                type: object
            required:
            - steps
            - summary
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
